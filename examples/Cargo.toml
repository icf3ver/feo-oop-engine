[package]
name = "examples"
version = "0.0.1"
authors = ["LittleTitan"]
edition = "2018"

[dependencies]

# TODO remove the need vulkano imports
vulkano = "0.22.0"
vulkano-shaders = "0.22.0"
winit = "0.24.0"
vulkano-win = "0.22.0"

# random dependancy
rand = "0.8.3"

# feo engine dependancies
feo-oop-engine = { path = "../feo-oop-engine" }
global-macro-derive = { path = "../feo-oop-engine/global-macro-derive" }

# feo math dependancy
feo-math = { git = "https://github.com/littleTitan/feo-math" }

# macro dependancies
macro_rules_attribute = "0.0.2"

[profile.release]
debug = true